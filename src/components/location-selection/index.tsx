import type { FC } from 'react'

interface LocationSelectionProps {}

export const LocationSelection: FC<LocationSelectionProps> = (props) => {
  return (
    <section>
      <header>
        <strong>Collge</strong>, location
      </header>
    </section>
  )
}

export default LocationSelection
